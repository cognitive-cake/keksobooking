'use strict';
// Задача: написать универсальный модуль для синхронизации любых полей
window.synchronizeFields = function (firstField, secondField, firstArrayOfValues, secondArrayOfValues, syncMethod) {
  firstField.addEventListener('change', function (evt) {

  });
};

// Пробовал написать функцию, но так и не понял, зачем передавать массивы со значениями (как указано в задании) и нужно ли. Я ведь работаю с селектами и мне нужен только порядковый номер опции для синхронизации. Решил не продолжать дальше самостоятельно, а дождаться сл. интенсива по JS
